# 页面标题
VITE_APP_TITLE = 用户端

# 生产环境配置
VITE_APP_ENV = 'production'

# 用户端/生产环境
VITE_APP_BASE_API = '/prod-api'

# 公开 API 客户端标识（随包分发、浏览器可见，仅作轻量接入标识，真实鉴权以后端为准）
VITE_APP_API_KEY = 'foundation'

# 是否在打包时开启压缩，支持 gzip 和 brotli
VITE_BUILD_COMPRESS = gzip
